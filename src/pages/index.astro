---
import Layout from "../layouts/Layout.astro";
import Navigation from "../components/Navigation.astro";
import NavigationIndictor from "../components/NavigationIndictor.astro";
import RightSideBar from "../components/RightSideBar.astro";
import LeftSideBar from "../components/LeftSideBar.astro";
---

<script>
  import gsap from "gsap";
  import { ScrollTrigger } from "gsap/ScrollTrigger";
  import { ScrollToPlugin } from "gsap/ScrollToPlugin";

  gsap.registerPlugin(ScrollTrigger);
  gsap.registerPlugin(ScrollToPlugin);

  const sections = gsap.utils.toArray(".section");
  const numSections = sections.length - 1;
  const snapVal = 1 / numSections;
  let lastIndex = 0;
  const navLis = document.querySelectorAll("li");

  gsap.to(sections, {
    xPercent: -100 * numSections,
    ease: "none",
    scrollTrigger: {
      trigger: ".sections-container",
      pin: true,
      scrub: true,
      snap: snapVal,
      // base vertical scrolling on how wide the container is so it feels more natural.
      end: () =>
        "+=" +
        ((document.querySelector(".sections-container") as HTMLElement)
          ?.offsetWidth -
          innerWidth),
      onUpdate: (self) => {
        const newIndex = Math.round(self.progress / snapVal);
        if (newIndex !== lastIndex) {
          navLis[lastIndex].classList.remove("active");
          navLis[newIndex].classList.add("active");

          lastIndex = newIndex;
        }
      },
    },
  });

  navLis.forEach((anchor, i) => {
    anchor.addEventListener("click", function (e) {
      gsap.to(window, {
        duration: 1,
        scrollTo: {
          y: i * innerWidth,
          autoKill: false,
        },
      });
    });
  });
</script>

<style>
  .sections-container {
    width: 400vw;
    height: 100vh;
    display: flex;
    flex-wrap: nowrap;
  }
  .section {
    width: 100vw;
    height: 100vh;
  }
</style>

<Layout title="Jim Luo">
  <Navigation />
  <NavigationIndictor />
  <div class="sections-container">
    <section class="section" id="section1">
      <!-- Main Content -->
      <main class="min-h-screen grid grid-cols-12 relative">
        <!-- Left sidebar -->
        <LeftSideBar />

        <!-- Main content -->
        <div class="col-span-8 relative">
          <div class="exhibition-content relative h-screen flex items-center">
            <div class="bg-[#2C3A2F] p-12 max-w-md relative z-10">
              <div class="text-white space-y-6">
                <p class="text-sm">MARCH 3, 2019</p>
                <h1 class="font-serif text-4xl">Prophecy of Colours</h1>
                <p class="text-lg font-serif">From Monet to Renoir</p>
                <div class="flex items-center space-x-4 mt-8">
                  <span class="text-sm">01</span>
                  <div class="flex-1 h-px bg-white/20"></div>
                  <span class="text-sm">04</span>
                </div>
                <div class="flex space-x-4 mt-4">
                  <button class="p-2 hover:bg-white/10 transition-colors">
                    <svg
                      class="w-6 h-6 text-white transform rotate-180"
                      fill="none"
                      stroke="currentColor"
                      viewBox="0 0 24 24"
                    >
                      <path
                        stroke-linecap="round"
                        stroke-linejoin="round"
                        stroke-width="2"
                        d="M9 5l7 7-7 7"></path>
                    </svg>
                  </button>
                  <button class="p-2 hover:bg-white/10 transition-colors">
                    <svg
                      class="w-6 h-6 text-white"
                      fill="none"
                      stroke="currentColor"
                      viewBox="0 0 24 24"
                    >
                      <path
                        stroke-linecap="round"
                        stroke-linejoin="round"
                        stroke-width="2"
                        d="M9 5l7 7-7 7"></path>
                    </svg>
                  </button>
                </div>
              </div>
            </div>
            <div class="absolute right-0 top-1/2 -translate-y-1/2 w-2/3 h-2/3">
              <img
                src="https://hebbkx1anhila5yf.public.blob.vercel-storage.com/image-AWWMlkssonBvSo29lvQm8M72dXnjO9.png"
                alt="Monet's Poppy Field"
                class="w-full h-full object-cover"
              />
            </div>
          </div>
        </div>
        <!-- Right sidebar -->
        <RightSideBar />
      </main>
    </section>

    <section class="section bg-gray-400" id="section2">
      <div class="h-full flex items-center justify-center">
        <h2 class="text-4xl font-serif">Collections</h2>
      </div>
    </section>

    <section class="section bg-gray-200" id="section3">
      <div class="h-full flex items-center justify-center">
        <h2 class="text-4xl font-serif">Events</h2>
      </div>
    </section>

    <section class="section bg-gray-300" id="section4">
      <div class="h-full flex items-center justify-center">
        <h2 class="text-4xl font-serif">Visit Us</h2>
      </div>
    </section>
  </div>
  <!-- <div class="min-w-screen min-h-screen">
    <RightSideBar />
  </div> -->
</Layout>
